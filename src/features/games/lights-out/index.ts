// Lights Out feature exports -- only the page component is part of
// the public API.  Internal modules (utils, types, config) should be
// imported directly when needed.
export { LightsOut } from './pages/LightsOut';
