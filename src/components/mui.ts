/**
 * Material-UI Components Barrel Export
 * Consolidates commonly used MUI components for cleaner imports
 */

// Core Layout Components
export { Grid } from '@mui/material';
export { Container, Box, Paper } from '@mui/material';

// Typography Components
export { Typography } from '@mui/material';

// Interactive Components
export {
    Button,
    IconButton,
    ToggleButton,
    ToggleButtonGroup,
} from '@mui/material';

// Navigation Components
export { Menu, MenuItem } from '@mui/material';

// Feedback Components
export { Tooltip, Fade, Backdrop, Modal, Alert } from '@mui/material';

// Layout Components
export { Divider } from '@mui/material';

// Data Display Components
export {
    Chip,
    Table,
    TableBody,
    TableCell,
    TableContainer,
    TableHead,
    TableRow,
    Link,
} from '@mui/material';

// Form Components
export { TextField } from '@mui/material';

// Theme Components
export {
    ThemeProvider,
    createTheme,
    styled,
    keyframes,
} from '@mui/material/styles';

// Core Styles
export { CssBaseline } from '@mui/material';

// Colors
export { grey, blueGrey } from '@mui/material/colors';

// Hooks
export { useMediaQuery } from '@mui/material';
